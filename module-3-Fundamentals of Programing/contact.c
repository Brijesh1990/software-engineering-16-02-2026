#include <stdio.h>
/*
what is constant ?
 constant never changed the values 
 constant never re-assigned the new values 
 contant always fixed never changed 
 const a=10;
 a=30;
*/

int main()
{
	const a=10;
	//a=40;
	printf("%d",a);
	return 0;
}
