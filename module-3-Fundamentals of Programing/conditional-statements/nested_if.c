#include <stdio.h>

int main()
{
  int a=10,b=5;
  if(a>b)
  {
    if(!a==0 && !b==0)
	{
	  printf("a and b both are positive numbers");	
	}	
  }	
  else 
  {
  	printf("a and b are not a positive numbers");
  }
  return 0;
}
